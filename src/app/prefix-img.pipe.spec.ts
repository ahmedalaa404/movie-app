import { PrefixImgPipe } from './prefix-img.pipe';

describe('PrefixImgPipe', () => {
  it('create an instance', () => {
    const pipe = new PrefixImgPipe();
    expect(pipe).toBeTruthy();
  });
});
